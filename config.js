'use strict';

module.exports = {
    port: 1234,
    dataFile: 'user_data.json',
    secretKey: 'son789&*(',
    tokenExpiredInSeconds: 7200, //in seconds, 2 hours,
    defaultPageSize: 4 //4 rows per page, easy for testing paging
};